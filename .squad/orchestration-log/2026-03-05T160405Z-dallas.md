# Orchestration Log: Dallas (Core Dev)
**Session:** 2026-03-05T16:04:05Z
**Mode:** background
**Task:** Library cleanup L1+L2+L3

## Assigned Work

Extract GetArgsParser/GetCommandParser to CommandInstruction base. Remove dead MountFlag parser from CMD/ENTRYPOINT. Delete dead TokenBuilder in FileTransferInstruction.

## Completed

**L1+L2:** `GetArgsParser(char)` and `GetCommandParser(char)` extracted from `CmdInstruction` and `EntrypointInstruction` to `CommandInstruction` as `protected static`. Removed dead `MountFlag.GetParser(escapeChar).Many()` combinator from extracted `GetArgsParser`. Added `private new static` annotations to `RunInstruction.GetArgsParser` and `RunInstruction.GetCommandParser` to suppress CS0108 warnings (intentional shadowing). Added `private new static` to `ShellInstruction.GetArgsParser`.

**L3:** Removed dead `TokenBuilder` construction block (9 lines) from `FileTransferInstruction.CreateInstructionString` (lines 126-135).

## Outcome

All 599 tests passing. Zero build warnings. Zero build errors.

## Files Changed

- `src/Valleysoft.DockerfileModel/CommandInstruction.cs` — added two `protected static` parser methods
- `src/Valleysoft.DockerfileModel/CmdInstruction.cs` — removed duplicate methods
- `src/Valleysoft.DockerfileModel/EntrypointInstruction.cs` — removed duplicate methods
- `src/Valleysoft.DockerfileModel/RunInstruction.cs` — added `new` keyword annotations
- `src/Valleysoft.DockerfileModel/ShellInstruction.cs` — added `new` keyword annotation
- `src/Valleysoft.DockerfileModel/FileTransferInstruction.cs` — removed dead TokenBuilder block
