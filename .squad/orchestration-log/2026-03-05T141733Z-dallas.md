# Dallas Orchestration Log

**Date:** 2026-03-05T14:17:33Z
**Agent:** Dallas (Core Dev)
**Task:** Implement ADD --checksum, --keep-git-dir, --link for issue #103
**Mode:** background
**Outcome:** SUCCESS

## Summary

Dallas completed implementation of three new options for the ADD instruction:
- `--checksum=<hash>` (key-value flag)
- `--keep-git-dir` (boolean flag)
- `--link` (boolean flag, reused from COPY)

## Files Created

- `src/Valleysoft.DockerfileModel/ChecksumFlag.cs` — new `KeyValueToken<KeywordToken, LiteralToken>` subclass
- `src/Valleysoft.DockerfileModel/KeepGitDirFlag.cs` — new `AggregateToken` subclass

## Files Modified

- `src/Valleysoft.DockerfileModel/AddInstruction.cs` — single public constructor, three new flag properties (Checksum, ChecksumToken, KeepGitDir, Link)
- `src/Valleysoft.DockerfileModel/DockerfileBuilder.cs` — AddInstruction builder extended with checksum, keepGitDir, link parameters

## Test Results

All 599 tests pass. 42 new tests added (Lambert).

## Decision Reference

Documented in `.squad/decisions.md` — "ADD --checksum, --keep-git-dir, --link Implementation Approach"

## Notes

LinkFlag reused from COPY --link implementation. No new class needed. Constructor refactored to single optional-parameter form to avoid overload ambiguity with named arguments in base-class factory lambda.
