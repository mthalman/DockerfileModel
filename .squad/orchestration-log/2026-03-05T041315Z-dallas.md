# Orchestration: Dallas (Core Dev)

**Timestamp:** 2026-03-05T04:13:15Z
**Task:** Implement --link option for COPY instruction (issue #115)
**Mode:** background
**Outcome:** SUCCESS

## Summary

Dallas successfully implemented the `--link` flag for the COPY instruction. This required establishing a new token pattern for valueless boolean flags, distinct from all existing value-based flags (`--from=`, `--chown=`, `--chmod=`).

## Artifacts

- **LinkFlag.cs** (new): Standalone AggregateToken implementation for the `--link` boolean flag
- **CopyInstruction.cs**: Extended GetInnerParser with LinkFlag alternative
- **FileTransferInstruction.cs**: Added optional `trailingOptionalFlag` parameter to CreateInstructionString for canonical flag ordering
- **DockerfileBuilder.cs**: Updated to support LinkFlag in COPY builder fluent API

## Tests

All 532 tests pass. No test failures.

## Design Decision

LinkFlag implemented as bare AggregateToken (not KeyValueToken), positioned last in canonical flag order: `--from`, `--chown`, `--chmod`, `--link`. Parser accepts arbitrary flag order; output enforces canonical order.

## Key Files Touched

- src/Valleysoft.DockerfileModel/LinkFlag.cs
- src/Valleysoft.DockerfileModel/CopyInstruction.cs
- src/Valleysoft.DockerfileModel/FileTransferInstruction.cs
- src/Valleysoft.DockerfileModel/DockerfileBuilder.cs
