# Orchestration Log: Dallas

**Date:** 2026-03-05T04:38:40Z
**Agent:** Dallas (Core Dev)
**Mode:** Background
**Task:** Implement --network and --security flags for RUN instruction (issue #116)

## Outcome: SUCCESS

### Work Summary
Implemented full support for `--network=<value>` and `--security=<value>` options on the RUN instruction, completing issue #116.

### Implementation Details

#### New Files Created
- `src/Valleysoft.DockerfileModel/NetworkFlag.cs` — KeyValueToken flag for --network
- `src/Valleysoft.DockerfileModel/SecurityFlag.cs` — KeyValueToken flag for --security

#### Files Modified
- `src/Valleysoft.DockerfileModel/RunInstruction.cs` — Added 3-tier property pattern for Network/Security, refactored parser to Options() pattern, updated constructors with optional network/security parameters
- `src/Valleysoft.DockerfileModel/DockerfileBuilder.cs` — Updated RunInstruction() builder methods with optional network/security parameters

### Architecture Decisions

**Flag Pattern:** Both flags implemented as `KeyValueToken<KeywordToken, LiteralToken>` subclasses, following the established pattern used by PlatformFlag, IntervalFlag, TimeoutFlag, etc.

**Parser Pattern:** RunInstruction.GetArgsParser() refactored from mount-only combinator to Options() method accepting MountFlag, NetworkFlag, or SecurityFlag in any order via `.Many().Flatten()`, matching HealthCheckInstruction pattern and preserving round-trip fidelity.

**Property Pattern:** Added string properties (Network, Security), literal token properties (NetworkToken, SecurityToken), and private flag token properties, following the exact 3-tier pattern from HealthCheckInstruction. Added `private readonly char escapeChar` field to RunInstruction.

**Constructor Changes:** Removed intermediate overloads taking `(string, IEnumerable<Mount>, char)` to avoid ambiguity with new optional-parameter overloads. New constructors fully subsume old ones with no breaking change.

### Test Results
- All 532 pre-existing tests pass
- Implementation ready for Lambert's test suite

### Files Affected
- `src/Valleysoft.DockerfileModel/NetworkFlag.cs` (new)
- `src/Valleysoft.DockerfileModel/SecurityFlag.cs` (new)
- `src/Valleysoft.DockerfileModel/RunInstruction.cs`
- `src/Valleysoft.DockerfileModel/DockerfileBuilder.cs`
