name: Release Drafter

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update_release_draft:
    uses: mthalman/release-automation/.github/workflows/release-draft.yml@89e88a5cf239f9d08369a43e9f0e0d603b48cb4a
